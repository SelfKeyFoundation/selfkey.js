/**
 * @constant
 * @private
 * @type object
 * @name JWT_ALGORITHMS
 * @memberof jwt
 * @property RSA - RSA algorithm name
 * @property HMAC - HMAC algorithm name
 */

export const JWT_ALGORITHMS = {
	RSA: 'RS512',
	HMAC: 'HS512'
};
