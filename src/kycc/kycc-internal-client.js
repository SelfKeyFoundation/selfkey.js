// const user = await request({
// 			url: `${KYCC_API_URL}/api/v2/auth/login`,
// 			method: 'POST',
// 			body: {
// 				jwt: token
// 			},
// 			json: true
// 		});
// 		return user.user;
// 	};

export const createExternalClient = (options = {}) => {
	// const {instanceUrl} = options;
};

export default createExternalClient;
