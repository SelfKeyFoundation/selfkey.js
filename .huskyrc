{
  "hooks": {
    "pre-commit": "yarn check-deps-precommit && pretty-quick --staged && yarn generate-readme && git add README.md && lint-staged",
    "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
  }
}
